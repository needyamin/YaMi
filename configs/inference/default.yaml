# Inference / generation defaults (overridable per request via the API).
inference:
  temperature: 0.8
  top_k: 50
  top_p: 0.95
  repetition_penalty: 1.1
  max_new_tokens: 256
  stop_sequences: []
  seed: null
  server_host: 127.0.0.1
  server_port: 8321
