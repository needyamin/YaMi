# Core runtime dependencies for Fontaine AI.
# CPU-only PyTorch is sufficient for the 16 GB RAM development setup:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
numpy>=1.24
pyyaml>=6.0
